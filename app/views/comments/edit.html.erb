<%= turbo_frame_tag "edit_comment" do %>
  <%= form_with model: [ @post, @comment ], url: [ @post, @comment ] do |f| %>
    <%= f.text_area :content, placeholder: @comment.content %><br>
    <%= f.submit "Submit" %>
  <% end %>
<% end %>
